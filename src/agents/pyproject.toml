[project]
name = "subagents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.5.7",
    "ipykernel>=7.2.0",
    "langchain>=1.2.18",
    "langchain-llama-server>=0.1.2",
    "langchain-mcp-adapters>=0.2.2",
    "mcp>=1.27.1",
    "pytest>=9.0.3",
    "rich>=15.0.0",
    "yapf>=0.43.0",
]

[project.scripts]
subagents  = "subagents:main"

[tool.uv.sources]
langchain-llama-server = { path = "../../../langchain-llama-server" }
